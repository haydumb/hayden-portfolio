apiVersion: v1
kind: Service
metadata:
  name: {{ include "hayden-portfolio.fullname" . }}
  labels:
    {{- include "hayden-portfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hayden-portfolio.selectorLabels" . | nindent 4 }}
